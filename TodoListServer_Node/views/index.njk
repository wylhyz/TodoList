<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- 上述3个meta标签*必须*放在最前面，任何其他内容都*必须*跟随其后！ -->
    <title>TodoList</title>
    <!-- Bootstrap CSS-->
    <link href="http://cdn.bootcss.com/bootstrap/3.3.6/css/bootstrap.min.css" rel="stylesheet">
    <link href="stylesheets/index.css" rel="stylesheet">
</head>
<body>

<div>
    <nav class="navbar navbar-default">
        <div class="navbar-header">
            <a class="navbar-brand" href="">Brand</a>
        </div>
    </nav>

    <!--展示数据-->
    <table class="table table-hover">
        <thead>
        <tr>
            <th>标题</th>
            <th>描述</th>
            <th>标记完成</th>
        </tr>
        </thead>
        <tbody>
        {% for todo in allContent %}
        <tr>
            <td>{{ todo.title }}</td>
            <td>{{ todo.description }}</td>
            <td>
                <label>
                    <input type="checkbox" disabled="disabled" value="{{ todo.id }}"
                           {% if todo.completed %}
                           checked="checked"
                           {% endif %}/>完成
                </label>
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

    <!--添加数据的表单-->
    <form action="/add" method="POST">
        <input type="text" placeholder="Todo标题(不可为空)" class='todo-input' name="title">
        <input type="text" placeholder="Todo描述" name="description">
        <button type="button submit" class="btn btn-primary" id="add">Add</button>
    </form>
</div>

<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
<script src="http://cdn.bootcss.com/jquery/3.1.0/jquery.min.js"></script>
<!-- Include all compiled plugins (below), or include individual files as needed -->
<script src="http://cdn.bootcss.com/bootstrap/3.3.6/js/bootstrap.min.js"></script>
<script type="text/javascript">
    $(document).ready(function () {
        $('#add').click(function (e) {
            if ($('.todo-input').val() == '') {
                e.preventDefault();
                alert('title不能为空');
            }
        });
    });
</script>
</body>
</html>